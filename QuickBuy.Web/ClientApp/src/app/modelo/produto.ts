export class Produto {
  id: number;
  nome: string;
  descricao: string;
  preco: number;
  nomeArquivo: string;

  precoOriginal: number;    // apenas para o template do carrinho de compras
  quantidade: number;

}
